import { RequestTransactionData } from './request-transaction-data';

describe('RequestTransactionData', () => {
  it('should create an instance', () => {
    expect(new RequestTransactionData()).toBeTruthy();
  });
});
