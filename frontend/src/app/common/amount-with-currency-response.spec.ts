import { AmountWithCurrencyResponse } from './amount-with-currency-response';

describe('AmountWithCurrencyResponse', () => {
  it('should create an instance', () => {
    expect(new AmountWithCurrencyResponse()).toBeTruthy();
  });
});
