import { RequestDataForReport } from './request-data-for-report';

describe('RequestDataForReport', () => {
  it('should create an instance', () => {
    expect(new RequestDataForReport()).toBeTruthy();
  });
});
