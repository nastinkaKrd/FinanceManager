import { RequestCategoryData } from './request-category-data';

describe('RequestCategoryData', () => {
  it('should create an instance', () => {
    expect(new RequestCategoryData()).toBeTruthy();
  });
});
